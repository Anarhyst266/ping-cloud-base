kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

configMapGenerator:
  - name: os-alerts-common
    files:
      - common.json
  - name: os-notifications-channels
    files:
      - destinations.json